<div class="table-container">
    <table class="table table-hover .table-responsive .table-sm w-auto small">
    <!-- <table class="table .table-responsive">     -->
        <tr>
            {% for col in column_names %}
            <th>{{col}}</th>


            <!-- {% if col != "id" %}
                <th>{{col}}</th>
            {% else %}
            {% endif %} -->
            {% endfor %}
        </tr>
        {% for row in row_data %}
        <!-- embedd if for color responsive -->
        <!-- and use if to hide columns -->
        <tr class="table-info">
            {% for col, row_ in zip(column_names, row) %}
            {% if col == "ID" %}
            <td style="visibility:collapse">{{row_}}</td>
            {% elif col == link_column %}
            <td>
                <button type="submit" value={{ row_ }} name="id form="patient_form" class="patient_button">
                <!-- <button type="submit" value="Test" "name="id form="patient_form" class="patient_button">    -->
                    {{ row_ }}
                </button>
            </td>
            {% else %}
            <td>{{row_}}</td>
            {% endif %}
            {% endfor %}
        </tr>
        {% endfor %}

    </table>
</div>